from multiprocessing import Pool

import start


def game():
    if __name__ == '__main__':
        with Pool(start.number_of_workers) as p:
            while len(mainnew.nameslist) < 200:
                p.imap(mainnew.generate_name, start.array_of_numbers)
